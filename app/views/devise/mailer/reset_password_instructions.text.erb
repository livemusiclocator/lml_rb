Hi <%= @resource.display_name.presence || @resource.email %>,

Someone requested a link to reset your Live Music Locator Backstage password. You can do this by visiting the link below.

<%= edit_password_url(@resource, reset_password_token: @token) %>

If you didn't request this, you can ignore this email. Your password won't change until you access the link above.

-- The LML team
https://livemusiclocator.com.au
