<% content_for :main do %>


    <main class="mx-auto max-w-5xl">
        <heading class='flex flex-col items-center '>
            <h1 class="font-bold text-4xl pb-8"><%= page_heading  %></h1>
            <%= yield (:extra_page_heading) %>
        </heading>
            <section class="prose ">

              <%= yield %>
            </section>
      </main>

<%  end %>

<%= render template: "web/layouts/application" %>
