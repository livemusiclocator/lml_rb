<div class="flex items-start space-x-3">
  <!-- Heroicon: musical-note -->
  <svg class="w-5 h-5 text-gray-400 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19V6l12-3v13M9 19c0 1.105-1.343 2-3 2s-3-.895-3-2 1.343-2 3-2 3 .895 3 2zm12-3c0 1.105-1.343 2-3 2s-3-.895-3-2 1.343-2 3-2 3 .895 3 2zM9 10l12-3"></path>
  </svg>
  <div class="flex-1">
    <h3 class="text-sm font-medium text-gray-900 mb-1">Artists & Sets</h3>
    <div class="space-y-2">
      <% gig[:sets].each do |set| %>
        <div class="text-sm">
          <div class="font-medium text-gray-900">
            <%= set[:artist] || set['artist'] %>
          </div>
          <% start_time = set[:start_time] || set['start_time'] %>
          <% duration = set[:duration] || set['duration'] %>
          <% if start_time.present? || duration.present? %>
            <div class="text-gray-600">
              <% if start_time.present? %>
                Start: <%= start_time %>
              <% end %>
              <% if duration.present? %>
                <%= " • " if start_time.present? %>Duration: <%= duration %>
              <% end %>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>